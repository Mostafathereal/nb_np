#include <vector>

// namespace math_core {
//     std::vector<int> fibonacci_sequence(int n);
//     void double_values(double* arr, size_t count);
// }

std::vector<int> fibonacci_sequence(int n);
void double_values(double* arr, size_t count);

